<div class="row-wrap nobor clearfix">
    <h1 class="title-score">Your Score: <%= session[:false_answer].to_i %> fout</h1>
    <div class="wrapper-span">
    	<div class="span4">
        	<div class="span-box">
            	<h2>0 tot 4 fout</h2>
                <div class="span-body">
                    Proficiat, u heeft de vragen goed genoeg beantwoord en wordt nu doorgeschakeld naar onze contactpagina zodat u uw gegevens kunt achterlaten. Wij nemen dan snel contact met u op.   
                </div>
            </div>
            <% if session[:false_answer].to_i < 4 %>
                <a href="<%= sollicitatie_formulier_action_path %>" class="org-btn">Naar sollicitatieformulier</a>                
            <% else %>
                <a href="<%= sollicitatie_formulier_action_path %>" class="org-btn" onclick="return false">Naar sollicitatieformulier</a>
            <% end %>         
        </div>
        <div class="span4">
        	<div class="span-box">
            	<h2>4 tot 6 fout</h2>
                <div class="span-body">
                </br></br>
                	Dit is helaas niet genoeg.</br> 
                    Probeer de test opnieuw.
                </div>
            </div>
            <% if (session[:false_answer].to_i >= 4)&&(session[:false_answer].to_i < 6)  %>
                <a href="<%= solliciteren_action_path %>" class="org-btn">Doe de test opnieuw</a>                
            <% else %>
                <a href="<%= solliciteren_action_path %>" class="org-btn" onclick="return false">Doe de test opnieuw</a>
            <% end %>
            
        </div>
        <div class="span4">
        	<div class="span-box">
            	<h2>6 tot 14 fout</h2>
                <div class="span-body">
                </br></br>
                	Blijf doen wat u doet en zie af van een carrière bij Dimarsec.
                </div>
            </div>
            <% if session[:false_answer].to_i >= 6  %>
                <a href="<%= root_path %>" class="org-btn">Escape and exit</a>                
            <% else %>
                <a href="<%= root_path %>" class="org-btn" onclick="return false">Escape and exit</a>
            <% end %>
        </div>
    </div>
</div>   